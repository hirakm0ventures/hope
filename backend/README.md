# Backend

NestJS + Prisma API for the waitlist and capacity engine.

For the full project setup, assignment notes, state machine, concurrency approach, expiration strategy, and test guide, see the repo root README:

- `README.md`

Useful backend commands:

```bash
pnpm install
pnpm run db:generate
pnpm run seed
pnpm run start:dev
pnpm run test:e2e
pnpm run build
```
